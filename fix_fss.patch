--- src/DHSIntegrationAgent.Workers/FetchStageService.cs
+++ src/DHSIntegrationAgent.Workers/FetchStageService.cs
@@ -104,9 +104,9 @@

         await using (var uow = await _uowFactory.CreateAsync(ct))
         {
-            var appSettings = await uow.AppSettings.GetAsync(ct);
-            if (appSettings.FetchClaimCountPerThread > 0)
+            var providerProfile = await uow.ProviderProfiles.GetActiveByProviderDhsCodeAsync(batch.ProviderDhsCode, ct);
+            if (providerProfile?.FetchClaimCountPerThread > 0)
             {
-                pageSize = appSettings.FetchClaimCountPerThread;
+                pageSize = providerProfile.FetchClaimCountPerThread;
             }
