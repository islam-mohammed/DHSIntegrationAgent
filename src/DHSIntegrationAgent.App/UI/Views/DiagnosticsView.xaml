<UserControl x:Class="DHSIntegrationAgent.App.UI.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:views="clr-namespace:DHSIntegrationAgent.App.UI.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:DHSIntegrationAgent.App.UI.Controls">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Diagnostics" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="API Call Log viewer - Recent 100 API calls." Foreground="{StaticResource BrushMuted}" Margin="0,4,0,0"/>
        </StackPanel>

        <TabControl Grid.Row="1"  Style="{StaticResource TabControlStyle}">
            <TabItem  Style="{StaticResource TabItemStyle}" >
                <!-- Header    -->
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <!-- Tab header icon -->
                            <materialDesign:PackIcon Kind="ClipboardPulseOutline"
                                            Style="{StaticResource TabHeaderIcon}" />
                            <TextBlock Style="{StaticResource TabHeaderTitle}" Padding="0,0,15,4" Text="Diagnostics" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>

                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <Grid Margin="15">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <StackPanel>
                        <TextBlock Text="Diagnostics" FontSize="20" FontWeight="SemiBold"/> 
                    </StackPanel> 
                    <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,12">
                        
                        <Button   FontSize="13.5" Height="33" Margin="5,0" BorderBrush="#32729B"  Foreground="#32729B"
                                  Style="{StaticResource MaterialDesignOutlinedButton}" materialDesign:ButtonAssist.CornerRadius="5" 
                                  Command="{Binding RefreshCommand}"
                                  IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" >
                            <StackPanel  Margin="0,0,0,0" Orientation="Horizontal" HorizontalAlignment="Left">
                                <materialDesign:PackIcon Kind="Sync" Width="17" Height="17" Margin="0,0,5,0"/>
                                <TextBlock  VerticalAlignment="Center" Foreground="#32729B" > Refresh</TextBlock>
                            </StackPanel>
                        </Button>
                        <Button FontSize="13.5"
                                     Height="34" Margin="5,0,0,0" 
                                     Cursor="Hand" Style="{StaticResource IconTextBtn}" 
                                    Command="{Binding ExportSupportBundleCommand}"
                                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                            <StackPanel  Margin="15,0,15,0" Orientation="Horizontal" HorizontalAlignment="Left">
                                <materialDesign:PackIcon Kind="ExportVariant" Width="16" Height="16" Margin="0,0,5,0"/>
                                <TextBlock  VerticalAlignment="Center" Foreground="#fff" > Export Support Bundle </TextBlock>
                            </StackPanel>
                        </Button> 
                    </StackPanel>

                    <Border Grid.Row="1" Background="{StaticResource BrushCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource Radius10}">
                        <Grid>
                            <Grid.RowDefinitions> 
                                <RowDefinition Height="*"/> 
                            </Grid.RowDefinitions>
                            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto"
                                 HorizontalScrollBarVisibility="Auto">
                                <DataGrid  ColumnHeaderStyle="{StaticResource TableHeaderStyle}"
                                    RowStyle="{StaticResource TableRowStyle}"
                                    CellStyle="{StaticResource TableCellStyle}"
                                    ItemsSource="{Binding ApiCalls}" AutoGenerateColumns="False" IsReadOnly="True" 
                                    CanUserAddRows="False"
                                    CanUserDeleteRows="False"
                                    SelectionMode="Single" 
                                    HeadersVisibility="Column" 
                                    Background="White"
                                    RowBackground="White"
                                    AlternatingRowBackground="#FFF9FAFB"
                                    HorizontalScrollBarVisibility="Auto"
                                    VerticalScrollBarVisibility="Auto"  
                                    GridLinesVisibility="None"
                                    HorizontalGridLinesBrush="Transparent"
                                    VerticalGridLinesBrush="Transparent"
                                    BorderThickness="0"     ColumnWidth="*" >
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="P.DHS Code" Binding="{Binding ProviderDhsCode}" Width="1" MinWidth="100"/>
                                        <DataGridTextColumn Header="Endpoint Name" Binding="{Binding EndpointName}" Width="2" MinWidth="100"/>
                                        <DataGridTextColumn Header="Correlation ID" Binding="{Binding CorrelationId}" Width="1.5" MinWidth="100"/>
                                        <DataGridTextColumn Header="Request UTC" Binding="{Binding RequestUtc}" Width="1.2" MinWidth="100"/>
                                        <DataGridTextColumn Header="Response UTC" Binding="{Binding ResponseUtc}" Width="1.2" MinWidth="100"/>
                                        <DataGridTextColumn Header="Duration (ms)" Binding="{Binding DurationMs}" Width="1" MinWidth="100"/>
                                        <DataGridTextColumn Header="HTTP Status" Binding="{Binding HttpStatusCode}" Width="1" MinWidth="100"/>
                                        <DataGridTextColumn Header="Succeeded" Binding="{Binding Succeeded}" Width="0.8" MinWidth="80"/>
                                        <DataGridTextColumn Header="Error Message" Binding="{Binding ErrorMessage}" Width="2.5" MinWidth="170"/>
                                        <DataGridTextColumn Header="Request Bytes" Binding="{Binding RequestBytes}" Width="1" MinWidth="100"/>
                                        <DataGridTextColumn Header="Response Bytes" Binding="{Binding ResponseBytes}" Width="1" MinWidth="100"/>
                                        <DataGridTextColumn Header="Was Gzip" Binding="{Binding WasGzipRequest}" Width="0.8" MinWidth="70"/>
                                    </DataGrid.Columns>
                                </DataGrid>

                            </ScrollViewer>
                            <!-- Loading Overlay -->
                            <controls:LoadingOverlay DataContext="{Binding}"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>
            <TabItem  Style="{StaticResource TabItemStyle}">
                <!-- Header    -->
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" >
                            <!-- Tab header icon -->
                            <materialDesign:PackIcon Kind="ServerOutline"
                                            Style="{StaticResource TabHeaderIcon}" />
                            <TextBlock Style="{StaticResource TabHeaderTitle}" Padding="0,0,15,4" Text="Log" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>

                    </DataTemplate>
                </TabItem.HeaderTemplate>
                <views:LogsView DataContext="{Binding LogsViewModel}"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
