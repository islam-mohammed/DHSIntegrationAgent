<UserControl x:Class="DHSIntegrationAgent.App.UI.Views.DiagnosticsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:DHSIntegrationAgent.App.UI.Controls">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,12">
            <TextBlock Text="Diagnostics" FontSize="20" FontWeight="SemiBold"/>
            <TextBlock Text="API call logs and failed claim details." Foreground="{StaticResource BrushMuted}" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Tabs -->
        <TabControl Style="{StaticResource TabControlStyle}" Grid.Row="1" Background="White" BorderThickness="0" Margin="0">

            <!-- ── API Call Logs ────────────────────────────────── -->
            <TabItem Style="{StaticResource TabItemStyle}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="NetworkOutline" Style="{StaticResource TabHeaderIcon}"/>
                            <TextBlock Style="{StaticResource TabHeaderTitle}" Padding="0,0,15,4" Text="API Call Logs" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <Grid Margin="15" Background="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tab header row -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="API Call Logs" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Text="Recent API calls ordered by request time." Foreground="{StaticResource BrushMuted}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <TextBlock Text="{Binding ApiHealthMessage}"
                                       VerticalAlignment="Center" Margin="0,0,10,0"
                                       FontSize="12" Foreground="Gray" TextWrapping="Wrap"/>
                            <Button FontSize="13.5" Height="33" Margin="5,0" BorderBrush="#0A7D7D" Foreground="#0A7D7D"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    materialDesign:ButtonAssist.CornerRadius="5"
                                    Command="{Binding CheckHealthCommand}"
                                    IsEnabled="{Binding IsCheckingHealth, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="HeartPulse" Width="17" Height="17" Margin="0,0,5,0"/>
                                    <TextBlock VerticalAlignment="Center" Foreground="#0A7D7D">Test API Health</TextBlock>
                                </StackPanel>
                            </Button>
                            <Button FontSize="13.5" Height="33" Margin="5,0" BorderBrush="#32729B" Foreground="#32729B"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    materialDesign:ButtonAssist.CornerRadius="5"
                                    Command="{Binding RefreshCommand}"
                                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Sync" Width="17" Height="17" Margin="0,0,5,0"/>
                                    <TextBlock VerticalAlignment="Center" Foreground="#32729B">Refresh</TextBlock>
                                </StackPanel>
                            </Button>
                            <Button FontSize="13.5" Height="34" Margin="5,0,0,0"
                                    Cursor="Hand" Style="{StaticResource IconTextBtn}"
                                    Command="{Binding ExportSupportBundleCommand}"
                                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Margin="15,0" Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="ExportVariant" Width="16" Height="16" Margin="0,0,5,0"/>
                                    <TextBlock VerticalAlignment="Center" Foreground="#fff">Export Support Bundle</TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Grid -->
                    <Border Grid.Row="2" Background="{StaticResource BrushCard}"
                            BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                            CornerRadius="{StaticResource Radius10}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <DataGrid ColumnHeaderStyle="{StaticResource TableHeaderStyle}"
                                          RowStyle="{StaticResource TableRowStyle}"
                                          CellStyle="{StaticResource TableCellStyle}"
                                          ItemsSource="{Binding ApiCalls}"
                                          AutoGenerateColumns="False" IsReadOnly="True"
                                          CanUserAddRows="False" CanUserDeleteRows="False"
                                          SelectionMode="Single" HeadersVisibility="Column"
                                          Background="White" RowBackground="White"
                                          AlternatingRowBackground="#FFF9FAFB"
                                          GridLinesVisibility="None" BorderThickness="0"
                                          ColumnWidth="*">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="P.DHS Code"    Binding="{Binding ProviderDhsCode}" Width="1"   MinWidth="100"/>
                                        <DataGridTextColumn Header="Endpoint Name" Binding="{Binding EndpointName}"   Width="2"   MinWidth="120"/>
                                        <DataGridTextColumn Header="Correlation ID" Binding="{Binding CorrelationId}"  Width="1.5" MinWidth="100"/>
                                        <DataGridTextColumn Header="Request UTC"   Binding="{Binding RequestUtc}"     Width="1.2" MinWidth="100"/>
                                        <DataGridTextColumn Header="Response UTC"  Binding="{Binding ResponseUtc}"    Width="1.2" MinWidth="100"/>
                                        <DataGridTextColumn Header="Duration (ms)" Binding="{Binding DurationMs}"     Width="1"   MinWidth="90"/>
                                        <DataGridTextColumn Header="HTTP Status"   Binding="{Binding HttpStatusCode}" Width="1"   MinWidth="80"/>
                                        <DataGridTextColumn Header="Succeeded"     Binding="{Binding Succeeded}"      Width="0.8" MinWidth="70"/>
                                        <DataGridTextColumn Header="Error Message" Binding="{Binding ErrorMessage}"   Width="2.5" MinWidth="160">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                        <DataGridTextColumn Header="Req Bytes"     Binding="{Binding RequestBytes}"   Width="1"   MinWidth="80"/>
                                        <DataGridTextColumn Header="Resp Bytes"    Binding="{Binding ResponseBytes}"  Width="1"   MinWidth="80"/>
                                        <DataGridTextColumn Header="Gzip"          Binding="{Binding WasGzipRequest}" Width="0.8" MinWidth="60"/>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                            <controls:PaginationBar Grid.Row="1" DataContext="{Binding ApiCallsPagination}"/>
                            <controls:LoadingOverlay Grid.Row="0" DataContext="{Binding}"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

            <!-- ── Failed Claims ───────────────────────────────── -->
            <TabItem Style="{StaticResource TabItemStyle}">
                <TabItem.HeaderTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <materialDesign:PackIcon Kind="AlertCircleOutline" Style="{StaticResource TabHeaderIcon}"/>
                            <TextBlock Style="{StaticResource TabHeaderTitle}" Padding="0,0,15,4" Text="Failed Claims" FontWeight="SemiBold" FontSize="14"/>
                        </StackPanel>
                    </DataTemplate>
                </TabItem.HeaderTemplate>

                <Grid Margin="15" Background="White">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Tab header row -->
                    <Grid Grid.Row="0" Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel>
                            <TextBlock Text="Failed Claims" FontSize="20" FontWeight="SemiBold"/>
                            <TextBlock Text="Claims that failed dispatch, ordered by last update." Foreground="{StaticResource BrushMuted}" Margin="0,4,0,0"/>
                        </StackPanel>
                        <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom">
                            <Button FontSize="13.5" Height="33" Margin="5,0" BorderBrush="#32729B" Foreground="#32729B"
                                    Style="{StaticResource MaterialDesignOutlinedButton}"
                                    materialDesign:ButtonAssist.CornerRadius="5"
                                    Command="{Binding RefreshFailedClaimsCommand}"
                                    IsEnabled="{Binding IsLoadingClaims, Converter={StaticResource InverseBooleanConverter}}">
                                <StackPanel Orientation="Horizontal">
                                    <materialDesign:PackIcon Kind="Sync" Width="17" Height="17" Margin="0,0,5,0"/>
                                    <TextBlock VerticalAlignment="Center" Foreground="#32729B">Refresh</TextBlock>
                                </StackPanel>
                            </Button>
                        </StackPanel>
                    </Grid>

                    <!-- Grid -->
                    <Border Grid.Row="2" Background="{StaticResource BrushCard}"
                            BorderBrush="{StaticResource BrushBorder}" BorderThickness="1"
                            CornerRadius="{StaticResource Radius10}">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="*"/>
                                <RowDefinition Height="Auto"/>
                            </Grid.RowDefinitions>
                            <ScrollViewer Grid.Row="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                                <DataGrid ColumnHeaderStyle="{StaticResource TableHeaderStyle}"
                                          RowStyle="{StaticResource TableRowStyle}"
                                          CellStyle="{StaticResource TableCellStyle}"
                                          ItemsSource="{Binding FailedClaims}"
                                          AutoGenerateColumns="False" IsReadOnly="True"
                                          CanUserAddRows="False" CanUserDeleteRows="False"
                                          SelectionMode="Single" HeadersVisibility="Column"
                                          Background="White" RowBackground="White"
                                          AlternatingRowBackground="#FFF9FAFB"
                                          GridLinesVisibility="None" BorderThickness="0"
                                          ColumnWidth="*">
                                    <DataGrid.Columns>
                                        <DataGridTextColumn Header="Claim ID"     Binding="{Binding ProIdClaim}"     Width="1"   MinWidth="90"/>
                                        <DataGridTextColumn Header="Company"      Binding="{Binding CompanyCode}"    Width="1"   MinWidth="80"/>
                                        <DataGridTextColumn Header="Month"        Binding="{Binding MonthKey}"       Width="0.8" MinWidth="70"/>
                                        <DataGridTextColumn Header="Batch ID"     Binding="{Binding BatchId}"        Width="0.8" MinWidth="70"/>
                                        <DataGridTextColumn Header="Attempts"     Binding="{Binding AttemptCount}"   Width="0.7" MinWidth="60"/>
                                        <DataGridTextColumn Header="Last Updated" Binding="{Binding LastUpdatedUtc}" Width="1.2" MinWidth="120"/>
                                        <DataGridTextColumn Header="Error"        Binding="{Binding LastError}"      Width="3"   MinWidth="200">
                                            <DataGridTextColumn.ElementStyle>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="TextWrapping" Value="Wrap"/>
                                                </Style>
                                            </DataGridTextColumn.ElementStyle>
                                        </DataGridTextColumn>
                                    </DataGrid.Columns>
                                </DataGrid>
                            </ScrollViewer>
                            <controls:PaginationBar Grid.Row="1" DataContext="{Binding FailedClaimsPagination}"/>
                        </Grid>
                    </Border>
                </Grid>
            </TabItem>

        </TabControl>

        <!-- Loading Overlay -->
        <controls:LoadingOverlay Grid.RowSpan="2" DataContext="{Binding}"/>
    </Grid>
</UserControl>
