<UserControl x:Class="DHSIntegrationAgent.App.UI.Views.MappingDomainView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:controls="clr-namespace:DHSIntegrationAgent.App.UI.Controls">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <StackPanel>
                <TextBlock Text="Mapping Domain" FontSize="20" FontWeight="SemiBold"/>
                <TextBlock Text="View domain mappings with provider and DHS values." Foreground="{StaticResource BrushMuted}" Margin="0,4,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Bottom"> 
                <Button   FontSize="13.5" Height="33" Margin="5,0" BorderBrush="#32729B"  Foreground="#32729B"
                             Style="{StaticResource MaterialDesignOutlinedButton}" materialDesign:ButtonAssist.CornerRadius="5" 
                              Command="{Binding RefreshCommand}" IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}"
                             >
                    <StackPanel  Margin="0,0,0,0" Orientation="Horizontal" HorizontalAlignment="Left">
                        <materialDesign:PackIcon Kind="Sync" Width="17" Height="17" Margin="0,0,5,0"/>
                        <TextBlock  VerticalAlignment="Center" Foreground="#32729B" > Refresh</TextBlock>
                    </StackPanel>
                </Button>

            </StackPanel>
        </Grid>

        <Border Grid.Row="1" Background="{StaticResource BrushCard}" BorderBrush="{StaticResource BrushBorder}" BorderThickness="1" CornerRadius="{StaticResource Radius10}" Padding="12">
            <DataGrid 
                  ColumnHeaderStyle="{StaticResource TableHeaderStyle}"
                  RowStyle="{StaticResource TableRowStyle}"
                  CellStyle="{StaticResource TableCellStyle}"
                    ItemsSource="{Binding MappingDomains}" 
                      AutoGenerateColumns="False" 
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      SelectionMode="Single" 
                      HeadersVisibility="Column"
                      Background="White"
                      RowBackground="White"
                      AlternatingRowBackground="#FFF9FAFB"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Auto"  
                      GridLinesVisibility="None"
                      HorizontalGridLinesBrush="Transparent"
                      VerticalGridLinesBrush="Transparent"
                      BorderThickness="0" > 

                <DataGrid.Columns>
                    <DataGridTextColumn Header="Provider Domain Code" 
                                        Binding="{Binding ProviderDomainCode}" 
                                        Width="*" 
                                        MinWidth="150"/>

                    <DataGridTextColumn Header="Provider Domain Value" 
                                        Binding="{Binding ProviderDomainValue}" 
                                        Width="*" 
                                        MinWidth="150"/>

                    <DataGridTextColumn Header="DHS Domain Value" 
                                        Binding="{Binding DhsDomainValue}" 
                                        Width="*" 
                                        MinWidth="150"/>

                    <DataGridTextColumn Header="Default?" 
                                        Binding="{Binding IsDefault}" 
                                        Width="80" 
                                        MinWidth="80"/>

                    <DataGridTextColumn Header="Code Value" 
                                        Binding="{Binding CodeValue}" 
                                        Width="*" 
                                        MinWidth="120"/>

                    <DataGridTextColumn Header="Display Value" 
                                        Binding="{Binding DisplayValue}" 
                                        Width="*" 
                                        MinWidth="150"/>
                </DataGrid.Columns>
            </DataGrid>
        </Border>
        
        <!-- Loading Overlay -->
        <controls:LoadingOverlay Grid.RowSpan="2" DataContext="{Binding}"/>
    </Grid>
</UserControl>
