<UserControl x:Class="DHSIntegrationAgent.App.UI.Views.ContactView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:DHSIntegrationAgent.App.UI.Views"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid Background="#fff" >
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel Margin="20">
                    <TextBlock Text="Need More Help?" 
                       FontSize="24" 
                       FontWeight="SemiBold" 
                       HorizontalAlignment="Center" 
                       Margin="0,0,0,20"/>

                    <Grid Margin="0" HorizontalAlignment="Stretch">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Address Card -->
                        <materialDesign:Card Style="{StaticResource ContactCardStyle}" Grid.Row="0" Grid.Column="0">
                        <DockPanel >
                            <materialDesign:PackIcon Kind="MapMarker"  Height="32" Width="32"
                                       VerticalAlignment="Center" Foreground="#32729B" Margin="15,0,15,0" />
                                <StackPanel VerticalAlignment="Center" >
                                    <TextBlock Text="Our Address" FontWeight="SemiBold" FontSize="16"/>
                                    <TextBlock Text="P.O. Box 50009, Riyadh, 11523, Saudi Arabia" 
                                       FontSize="14" 
                                       Foreground="#555"/>
                                </StackPanel>
                            </DockPanel>
                        </materialDesign:Card>

                        <!-- Phone Card -->
                        <materialDesign:Card Style="{StaticResource ContactCardStyle}" Grid.Row="0" Grid.Column="1">
                            <DockPanel>
                            <materialDesign:PackIcon Kind="Phone" Height="32" Width="32"
                                       VerticalAlignment="Center" Foreground="#32729B" Margin="15,0,15,0"  />
                            <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Call Us" FontWeight="SemiBold" FontSize="16"/>
                                    <TextBlock Text="Customer Support" FontSize="14" Foreground="#555"/>
                                    <TextBlock>
                                        <Hyperlink >
                                            920000958
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </DockPanel>
                        </materialDesign:Card>

                        <!-- Email Card (Full Width) -->
                        <materialDesign:Card Style="{StaticResource ContactCardStyle}" Grid.Row="1" Grid.ColumnSpan="2">
                            <DockPanel>
                            <materialDesign:PackIcon Kind="Email"  Height="32" Width="32"
                                       VerticalAlignment="Center" Foreground="#32729B" Margin="15,0,15,0"  />
                            <StackPanel VerticalAlignment="Center">
                                    <TextBlock Text="Email Us" FontWeight="SemiBold" FontSize="16"/>
                                    <TextBlock Text="We’d love to hear from you" FontSize="14" Foreground="#555"/>
                                    <TextBlock>
                                        <Hyperlink >
                                            customercare@dhsarabia.com
                                        </Hyperlink>
                                    </TextBlock>
                                </StackPanel>
                            </DockPanel>
                        </materialDesign:Card>

                    </Grid>
                </StackPanel>
            </ScrollViewer>

    </Grid>
</UserControl>
